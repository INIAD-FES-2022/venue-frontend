import { style } from '@vanilla-extract/css';

export const wrapper = style({
  margin: 'auto',
  maxWidth: '800px',
  fontSize: '1rem',
});
export const sectionTitle = style({
  fontSize: '1.2rem',
  textAlign: 'left',
});
