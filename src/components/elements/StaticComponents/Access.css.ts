import { style } from '@vanilla-extract/css';

export const accessStyle = {
  wrapper: style({
    margin: 'auto',
    width: '800px',
    fontSize: '1rem',
    textAlign: 'left',
    padding: '40px',
  }),

  sectionTitle: style({
    fontSize: '1.5rem',
  }),
};
